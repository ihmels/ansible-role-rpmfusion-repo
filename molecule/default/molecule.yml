---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: "${MOLECULE_IMAGE:-docker.io/geerlingguy/docker-fedora33-ansible:latest}"
    pre_build_image: true
provisioner:
  name: ansible
