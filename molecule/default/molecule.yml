---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: instance
    image: "${MOLECULE_IMAGE:-docker.io/geerlingguy/docker-fedora38-ansible:latest}"
    pre_build_image: true
provisioner:
  name: ansible
